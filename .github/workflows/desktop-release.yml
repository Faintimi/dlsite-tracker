# 桌面版打包（Windows NSIS + macOS .app）：产出安装包 / 应用包，供下载与验收测试。
# 触发：手动触发 / 推送 desktop/** 到 main（仅上传产物）/ 推送 desktop-v* 标签（同时附加到 Release）。
name: Desktop Build (Windows / macOS)

on:
  workflow_dispatch:
  push:
    branches: [main]
    tags: ["desktop-v*"]
    paths:
      - "desktop/**"
      - "dlsite_tracker/**"
      - "packaging/**"
      - "scripts/build-sidecar.sh"
      - ".github/workflows/desktop-release.yml"

permissions:
  contents: read

concurrency:
  group: desktop-build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  windows:
    runs-on: windows-latest
    timeout-minutes: 90
    defaults:
      run:
        working-directory: desktop
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: desktop/package-lock.json

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@6bed0761d98439e5a578e2877258200ad565ba87 # stable

      - name: Rust cache
        uses: swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2
        with:
          workspaces: desktop/src-tauri -> target

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npm run check

      - name: Build embedded pipeline (sidecar)
        shell: bash
        working-directory: ${{ github.workspace }}
        run: bash scripts/build-sidecar.sh

      - name: Build installer (NSIS)
        run: npm run tauri build -- --bundles nsis

      - name: Upload installer artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: doujin-game-radar-windows-installer
          path: desktop/src-tauri/target/release/bundle/nsis/*.exe
          if-no-files-found: error

  macos:
    runs-on: macos-latest
    timeout-minutes: 90
    defaults:
      run:
        working-directory: desktop
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: desktop/package-lock.json

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@6bed0761d98439e5a578e2877258200ad565ba87 # stable

      - name: Rust cache
        uses: swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2
        with:
          workspaces: desktop/src-tauri -> target

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npm run check

      - name: Build embedded pipeline (sidecar)
        shell: bash
        working-directory: ${{ github.workspace }}
        run: bash scripts/build-sidecar.sh

      - name: Build app bundle
        run: npm run tauri build -- --bundles app

      - name: Package zip
        run: |
          cd src-tauri/target/release/bundle/macos
          ditto -c -k --keepParent "Doujin Game Radar.app" "../DoujinGameRadar-macos.zip"

      - name: Upload app artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: doujin-game-radar-macos-app
          path: desktop/src-tauri/target/release/bundle/DoujinGameRadar-macos.zip
          if-no-files-found: error

  release:
    if: startsWith(github.ref, 'refs/tags/desktop-v')
    needs: [windows, macos]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          path: dist-artifacts

      - name: Attach packages to release
        uses: softprops/action-gh-release@efb35369e0ad2afab669f228072c1b0d510eae64 # v3.0.3
        with:
          files: |
            dist-artifacts/doujin-game-radar-windows-installer/*.exe
            dist-artifacts/doujin-game-radar-macos-app/*.zip
          name: "Doujin Game Radar ${{ github.ref_name }}"
          body: |
            同人游戏雷达 · Doujin Game Radar —— 桌面版（Windows 安装包 / macOS 应用包）。

            - Windows：NSIS `.exe` 安装包（Windows 10+，WebView2 由安装器处理）
            - macOS：`.app` 压缩包，解压后拖入「应用程序」即可（未签名，首次打开如被拦截请右键→打开）
            - 打包版内置数据管道，使用「更新数据」无需安装 Python（见仓库 README）
            - Windows installer (NSIS) and macOS `.app` bundle for the local-first
              DLsite doujin game browser. The packaged app includes the pipeline;
              Python is not required for in-app updates.
